package stepsdefinitions;

import factory.PageFactoryManager;
import org.junit.Test;
import pages.LoginPage;

public class Login_Steps {
    /*private LoginPage loginPage;
    private final TestContext context;
    public Login_Steps() {
        this(new TestContext()); // Use default TestContext or provide your own
        loginPage = PageFactoryManager.getLoginPage(context.driver);
    }

    /*public Login_Steps(TestContext context) {
        this.context = context;
        loginPage = PageFactoryManager.getLoginPage(context.driver);
    }*/

    // TODO
    // Here the methods of steps definitions

    /*@Test
    public void first_test() throws InterruptedException {
        loginPage.clickOnLogin();
    }*/
}
